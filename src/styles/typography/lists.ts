import * as stylex from "@stylexjs/stylex";

export const dl = stylex.create({
  title: {
    fontSize: 14,
    fontWeight: 600,
    color: "rgba(255, 255, 255, 0.5)",
  },
  definition: {
    fontSize: 12,
    color: "rgba(255, 255, 255, 0.3)",
  },
});
